# Core commit whose published Mattermost image all E2E jobs use when MM_IMAGE is unset. The
# harness derives the image tag from the first seven characters of this sha.
#
# Keep the pin in this repository because E2E behavior can depend on integrated Mattermost changes
# outside the server/public Go module. Update it whenever the E2E suite requires newer core
# behavior.
#
# Core CI's per-commit images are not retained indefinitely. When this tag stops resolving, update
# the pin to a commit whose image is still published — verify with
#   curl -s -o /dev/null -w '%{http_code}' \
#     https://hub.docker.com/v2/repositories/mattermostdevelopment/mattermost-team-edition/tags/<7-char-sha>/
#
# One sha, comments and blank lines ignored.
4a0b9bde1b925f0a9e51c333658cbc658dfe9c33
