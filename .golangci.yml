version: "2"

run:
  timeout: 5m
  modules-download-mode: readonly
  # The e2e suite is behind a build tag, so without it every linter — including the
  # skip ban below — sees a package with no buildable files and reports nothing.
  build-tags:
    - e2e

linters:
  default: none
  enable:
    - bidichk
    - bodyclose
    - errcheck
    - forbidigo
    - gocritic
    - gosec
    - govet
    - ineffassign
    - makezero
    - misspell
    - modernize
    - nakedret
    - revive
    - staticcheck
    - unconvert
    - unqueryvet
    - unused
    - whitespace
  settings:
    forbidigo:
      # Tests must never pass by skipping. Ban every skip mechanism so the no-skip
      # rule cannot be reintroduced per-test; a missing test DB must fail, not skip.
      forbid:
        - pattern: '\.Skip(f|Now)?$'
          msg: 'tests must never skip; provide a Postgres DSN (TEST_DATABASE_POSTGRESQL_DSN) so the test runs'
        - pattern: '^testing\.Short$'
          msg: 'do not gate tests on -short; tests must always run'
      analyze-types: true
    govet:
      enable-all: true
      disable:
        - fieldalignment
    revive:
      rules:
        - name: exported
          disabled: true
        - name: unused-parameter
          disabled: true
    unqueryvet:
      check-sql-builders: true
  exclusions:
    paths:
      - webapp
    rules:
      - path: server/configuration.go
        linters:
          - unused
      - path: _test\.go
        linters:
          - bodyclose

formatters:
  enable:
    - gofmt
    - gofumpt
    - goimports
  settings:
    gofmt:
      simplify: true
      rewrite-rules:
        - pattern: interface{}
          replacement: any
    goimports:
      local-prefixes:
        - github.com/mattermost/mattermost-plugin-docs
